export type TripResponse = {
    id: string;
    title: string;
    description: string;
    // search: string;
    // sort: string;
    // href: string;

    // page: number;
    // pageSize: number;
    // include: string;
}