export type TripResponse = {
    createdAt: string;
    id: string;
    title: string;
    description: string;
    placesCount: number;
    userId: string;

    // sort: string;
    // href: string;

    // page: number;
    // pageSize: number;
    // include: string;
}